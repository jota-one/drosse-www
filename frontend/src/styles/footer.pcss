footer {
    background: var(--c-white);

    .dark & {
        background: var(--c-gray-darker);
    }
    
    .container {
        display: flex;
        flex-direction: row;
        alig-items: center;
        padding: 3rem;
        border-top: 1px dashed gray;
    }

    a {
        color: var(--c-green);

        &:hover {
            color: var(--c-gray);

            .dark & {
                color: var(--c-almost-light);
            }
        }

        &:first-child {
            margin-right: 1rem;
        }
    }
}